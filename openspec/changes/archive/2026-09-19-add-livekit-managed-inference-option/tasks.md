# Tasks: Add LiveKit Managed Inference Option & Native SFU Metrics

## 1. Spec & Documentation Alignment

- [x] 1.1 Document LiveKit Managed Inference provider option and native SFU Prometheus metric scraping in `README.md` and `workflow.md`.
- [x] 1.2 Audit `VoicePipelineFactory` and `prometheus.yml` for optional LiveKit Managed Inference settings.
