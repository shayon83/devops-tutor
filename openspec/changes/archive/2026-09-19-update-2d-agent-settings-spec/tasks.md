# Tasks

## 1. Spec & Configuration Alignment

- [ ] 1.1 Review `.env.example` and `README.md` to confirm `VOICE_PIPELINE_MODE` (`realtime` | `modular`) and `AGENT_DISPATCH_TYPE` (`local` | `cloud`) grouping is documented cleanly and verify with `openspec validate`.
- [ ] 1.2 Sync spec delta for `update-2d-agent-settings-spec` to main spec using `openspec sync` and archive using `openspec archive`.
