schema: spec-driven

# Project context
context: |
  DevOps LLM Voice Tutor Web Application
  - Stack: LiveKit Agents WebRTC, FastAPI, Redis 7, React + Vite, Prometheus, Loki, Grafana
  - Architecture: 2D Orthogonal Matrix (VOICE_PIPELINE_MODE=realtime|modular, AGENT_DISPATCH_TYPE=local|cloud)
  - Persona: Socratic SRE Tutor with real-time WebRTC DataTrack visual workspace (Mermaid.js + YAML cards)
  - Deployment: Single-command Docker Compose (`docker compose up --build`), zero hardcoded secrets (.env)
